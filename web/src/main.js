// Entry point for the bundled front-end. Importing a component registers its
// custom element as a side effect; re-exporting keeps the symbols available to
// unit tests and any future programmatic use.
export * from './components/dsn-status.js'
